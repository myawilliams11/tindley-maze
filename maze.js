const mazeDiv = document.getElementById("mazeDiv");
const avatar = document.getElementById("avatar");

// Size of the squares in the grid, in pixels.
const squareSize = 33;

// START HERE

// Using the map in map.js, create a maze game. "W" represents walls, "S"
// represents the player's start position, "F" represents the finish position

document.addEventListener('keydown', (event) => {
    const keyName = event.key;

    // Implement logic to move the avatar around when the user presses an arrow
    // key. keyName will be one of "ArrowLeft", "ArrowRight", "ArrowUp",
    // "ArrowDown".

});
